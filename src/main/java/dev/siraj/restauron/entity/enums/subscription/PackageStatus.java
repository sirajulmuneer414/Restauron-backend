package dev.siraj.restauron.entity.enums.subscription;

// Package status enum - ACTIVE, HIDDEN, ARCHIVED - for subscription packages
public enum PackageStatus {
    ACTIVE,
    HIDDEN,
    ARCHIVED
}
